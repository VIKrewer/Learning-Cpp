#include <iostream>
#include <cmath>

int main(){
    double x = 3.14159;
    double y = 4;
    double z;

    // z = std::max(x, y); result = 4
    // z = std::min(x, y); result = 3.14159

    // z = pow(2,3); result = 9

    // z = sqrt(9); result = 3

    // z = abs(-32); result = +32

    // z = round(x); result = 3

    // z = ceil(x); result = 4 (will always round up)

    // z = floor(x); result = 3 (will always round down)

    std::cout << z;

    return 0;
}